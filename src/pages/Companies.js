import React from "react";
import { ReactDOM } from "react";

class Companies extends React.Component{
    state = {
        companies: [],
    }

    // use componentDidMount
    // call fetch(), call function companyCard()
    // make <Company />

    render() {
        return(
            <div>

            </div>
        );
    }
}

export default Companies;